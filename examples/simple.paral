version   = "1.23.0"
platforms = ["linux"]::["amd64", "arm64"]

notification:
    > curl -H 'Content-Type: application/json' "https://some.notification.com/webhook/v2/xyz"

install:
    > bun run install

build:
    > @once @print("Building App")
    > bun run build
    > @tigger(notification)
