url      = "https://google.com"
items    = ["ubuntu", "fedora"] :: ["noble", "jammy"]

@id "scrap"
@for items
scrap:
    -> @stash["first_stash"] << @sprintf("This is a stash pipeline for index %s-%s", @first(@value), @last(@value))
    -> echo @sprintf(@stash("first_stash"))
