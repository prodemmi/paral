url = "https://google.com"
filename = "output.txt"


task "scrap" {
    -> @buf["first_stash"] << curl -s https://google.com
}


task "scrap2" {
    -> sleep 3
    -> echo @sprintf(@buf("first_stash"))
}