x = @sprintf("Hi")

task add_row {
    -> sleep 5
    -> @stash["x"] << cat simple.txt | grep email
    -> @buf["x"] << x
}

@wait @isnot(@buf("x"), hich)
task show_matrix {
    -> @print(@buf("x"))
}

@depend add_row
task depended_task {
    -> @print(@stash("x"))
}