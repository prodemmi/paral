version = @sprintf("v1.0.1")

@id "build_app"
@manual
@description "It build the app version"
build:
    -> @printf("🔧 Building the app %s...", version)

@id "build_web"
@description "It build the web version"
@manual
build:
    -> @printf("🔧 Building the web %s...", version)

@id "start_build"
start_build:
    -> @trigger(build_app)
    -> @trigger(build_web)
