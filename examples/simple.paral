files = ["document.txt", "image.png", "notes.txt", "script.py", "readme.txt"]

@for files
task process_files {
    -> @if(@contains(@value, ".py")) {
        -> @stash["python_file"] << @sprintf(@value)
        -> python3 @stash("python_file")
    }
}
