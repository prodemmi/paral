LINUX_ARCH ["linux"] :: ["amd64", "arm64"]

@name "Compile"
COMPILE:
    @stash[output]
    @print("Compiling for @1 @2")

@depend COMPILE
@name "Test"
TEST:
    @print("Testing with output: @stash.output")